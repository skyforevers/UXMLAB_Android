<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hh960.uxmlab">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".object.GlobalIdApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activityPackage.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activityPackage.CourseListActivity" />
        <activity android:name=".activityPackage.SignupAcitivty"/>
        <activity android:name=".activityPackage.AddCourseActivity"/>
        <activity android:name=".activityPackage.MenuActivity"/>
        <activity android:name=".activityPackage.CourseActivity"/>
        <activity android:name=".activityPackage.BoardWriteActivity"/>
    </application>

</manifest>